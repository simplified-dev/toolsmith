{
  "//": "Rename to .mcp.json and place at W:/Workspace/Java/Simplified/ so these tool schemas load ONLY under the Java workspace, not in every session (avoids the per-turn always-on tax). Adjust the script path to where you install DRAFT-simplified-tools-mcp-server.py.",
  "mcpServers": {
    "simplified-tools": {
      "command": "python",
      "args": ["W:/Workspace/Java/Simplified/tools/simplified-tools-mcp-server.py"],
      "env": {
        "SIMPLIFIED_ROOT": "W:/Workspace/Java/Simplified"
      }
    }
  }
}
