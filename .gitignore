# Python
__pycache__/
*.py[cod]
*.egg
*.egg-info/
.eggs/
build/
dist/
wheels/

# Virtual environments
.venv/
venv/
env/
.env

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.coverage
htmlcov/

# IDE / OS
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# Per-workspace discovery cache (written by `toolsmith setup` at the SCANNED root,
# not here; this guard is only for a stray cache at the repo root).
/.toolsmith/

# Local scratch: the token-optimization audit, drafts and raw session-derived
# evidence. Kept on disk for reference; never committed/pushed.
/notes/
